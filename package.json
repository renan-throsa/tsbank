{  
  "name": "tsbank",
  "version": "1.0.0",
  "description": "Aplicação para aprendizagem de typescript.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "start": "tsc -w",
    "predeply": "npm run compile",
    "deploy": "gh-pages -d app"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^13.13.4",
    "gh-pages": "^3.1.0",
    "lite-server": "^2.5.4",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "@types/jquery": "^3.3.36",
    "concurrently": "^5.2.0"
  }
}